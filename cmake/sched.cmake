# xsprof sched collector module (Phase 2 — placeholder).
# When the scheduler collector lands, add its sources here:
#   set(XSPROF_SCHED_SOURCES src/sched/collector.cpp)
set(XSPROF_SCHED_SOURCES "")
