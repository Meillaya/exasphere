# xsprof pipeline module (aggregation + sink interface).
# Sources are already in core.cmake (src/pipeline/pipeline.cpp).
# This file exists so the pipeline lane has a dedicated entry point.
set(XSPROF_PIPELINE_SOURCES "")
