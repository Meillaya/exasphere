# xsprof memory collector module (Phase 2 — placeholder).
# When the memory collector lands, add its sources here:
#   set(XSPROF_MEMORY_SOURCES src/memory/collector.cpp)
set(XSPROF_MEMORY_SOURCES "")
