# zig-scheduler

`zig-scheduler` is a deterministic CPU scheduling simulator written in Zig for experimenting with scheduling policies, scenarios, traces, and offline observability fixtures. It focuses on repeatable simulator runs, policy comparison, and lightweight teaching/research workflows rather than acting as a live kernel scheduler or production scheduling daemon.
