# Fixture-only disposable VM harness config for T15 QA.
# Parsed as data by qa/vm/run_lab.sh; never sourced as shell.
ZIG_SCHEDULER_VM_IMAGE=fixtures/vm/test-image.raw
ZIG_SCHEDULER_VM_DRIVER=fixture
ZIG_SCHEDULER_VM_TEST_FIXTURE=1
