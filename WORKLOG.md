# WORKLOG

This worklog was reset on `2026-06-12T12:49:22-04:00`.

Going forward, every meaningful feature, verification checkpoint, commit/push checkpoint, safety decision, or plan milestone will be recorded here with an ISO-8601 timestamp and a concise description of the work completed.

## Timestamped milestones

- `2026-06-12T12:49:22-04:00` — **Worklog reset:** Cleared the previous historical worklog and started a new milestone log. Current active plan remains `.omo/plans/tui-driven-live-lab-scheduler.md`; completed plan items before this reset were T01–T08, and the next planned item is T09. This entry is not a production-readiness claim.
- `2026-06-12T12:53:21-04:00` — **GitHub language and local artifact policy:** Added `.gitattributes` Linguist rules so GitHub language statistics count Zig/ZON as Zig and exclude non-Zig QA/docs/evidence files from the language bar. Rebuilt `.gitignore` around Zig build outputs, local workflow state, Python caches, local verification scratch outputs, and local-only scratch scripts while keeping tracked governance and QA sources available.
- `2026-06-12T12:56:45-04:00` — **Commit/push checkpoint for language policy:** Prepared the `.gitattributes`, `.gitignore`, worklog reset, and governance manifest hash updates for a tracked commit to `master` so GitHub can recalculate the repository language bar from the pushed attributes.
- `2026-06-12T13:11:38-04:00` — **T09 TUI-to-daemon action dispatch:** Wired interactive TUI actions through the disabled-safe daemon stdin adapter using typed operator action JSON. Pressing `v` now dispatches `verifier_only` to `zig-scheduler-daemon`, records a fail-closed daemon journal with `host_mutation=false`, and renders `verifier queued/refused host-safe` in the TUI. Malformed/missing daemon responses display `daemon unsafe_to_assume` instead of crashing.
- `2026-06-12T13:18:00-04:00` — **T09 verification complete:** Independent ultrawork verifier confirmed the TUI-to-daemon path after tightening daemon event parsing against misleading daemon output. Final T09 evidence includes passing Zig tests, governance/release gates, manual tmux TUI and daemon journals, and a fake mismatched-daemon probe that renders `daemon unsafe_to_assume`.
- `2026-06-12T13:27:30-04:00` — **T10 host-safe lab harness from daemon:** Added daemon execution of the trusted `run_lab_host_safe` command registry path. The daemon now runs the host-safe run-all harness, writes per-stage PASS/SKIP/REFUSE events into its event journal, and leaves host mutation false while generating a validator-checked `evidence/lab/run-all/<run-id>/summary.json`.
- `2026-06-12T13:36:00-04:00` — **T10 verification complete:** Independent ultrawork verifier confirmed daemon-driven host-safe lab harness execution. The daemon journal records every run-all stage as PASS/SKIP/REFUSE with `host_mutation=false`, summary validation passes, hostile PATH does not intercept execution, and generated local evidence was cleaned before commit.
- `2026-06-12T13:52:00-04:00` — **T11 append-only daemon journal:** Added typed action IDs and daemon journal replay tracking. The daemon now records accepted action metadata, refuses duplicate action IDs in the same run and across replay, rejects corrupt/stale/non-contiguous existing journals, and preserves `host_mutation=false` in all journal/refusal paths.
- `2026-06-12T14:22:44-04:00` — **T12 runtime stream reader:** Added a daemon `--stream-runtime` mode backed by a sanitized runtime JSONL fanout model. The daemon now emits accepted `runtime_sample` events, marks malformed/private/stale samples `unsafe_to_assume`, supports replay with `--stream-from`, bounds stream output with a drop incident, persists the event journal, and keeps every event `host_mutation=false`.
- `2026-06-12T14:27:39-04:00` — **T12 verification complete:** Root gates and independent ultrawork verification confirmed the runtime stream path. Evidence includes Zig tests, runtime sample validator, governance and release gates, manual daemon stream probe, stale-git rejection, replay/resume with `--stream-from`, persisted-journal equality, and bounded backpressure/drop behavior.
- `2026-06-12T14:30:57-04:00` — **T12 commit/push checkpoint and T13 daemon packaging:** Pushed T12 as `058e87f feat(observe): stream runtime samples through daemon`, then added T13 package staging for `zig-scheduler-daemon` and a disabled/manual-only `zig-scheduler-daemon.service`. Package defaults and manifest validation now require the daemon binary/unit while preserving no-auto-start and no scheduler mutation defaults.
- `2026-06-12T14:35:00-04:00` — **T13 verification complete:** Independent ultrawork verification confirmed package staging now includes the disabled/manual-only daemon binary and systemd unit. The package manifest, defaults gate, tamper rejection, release gate, and direct unit inspection passed with no auto-start, no daemon `WantedBy=`, no mutation service install-enable, and no scheduler mutation commands executed.
- `2026-06-12T14:41:19-04:00` — **T14 daemon/TUI no-host safety matrix:** Expanded the safety matrix to cover daemon `partial_attach` and `rollback` refusals plus the TUI-to-daemon verifier path under the no-host mutation audit. Added hostile PATH daemon interception coverage and confirmed ambient no-strace bypass rejection remains fail-closed.
- `2026-06-12T14:41:19-04:00` — **T14 verification complete:** Independent ultrawork verification confirmed `qa/unsafe_cli_matrix.sh`, `qa/no_host_mutation.sh`, Zig tests, governance, release gate, strace-backed no-host audit, no-strace bypass rejection, and no leftover daemon/TUI processes.
- `2026-06-12T14:46:15-04:00` — **T15 disposable VM execution harness:** Replaced the pre-T15 execute refusal with an explicit-config execution harness. Execute mode now refuses missing/invalid/ambiguous config, skips missing QEMU/KVM safely, and supports a tracked fixture VM config that exercises copy-in, VM marker probing, transcript creation, copy-out, and teardown receipts without claiming VM-live or release-eligible proof.
- `2026-06-12T14:50:29-04:00` — **T15 verification complete:** Independent ultrawork verification confirmed execute mode fail-closed behavior, fixture VM marker transcript/copy-out/cleanup evidence, invalid/ambiguous config rejection, env-file non-sourcing, no QEMU leftovers, Zig tests, governance, and release gate. The fixture remains non-VM-live and not release-eligible proof.
- `2026-06-12T14:55:09-04:00` — **T16 VM marker attestation and tuple validation:** Added VM attestation JSON validation and integrated fixture attestation into the disposable VM harness. The validator requires the VM marker, current git SHA, guest copy-out provenance, supported kernel tuple, BTF, BPF JIT, `CONFIG_SCHED_CLASS_EXT`, and no host `/sys` source path; malicious host attestation fixtures are rejected.
- `2026-06-12T14:58:43-04:00` — **T16 verification complete:** Independent ultrawork verification confirmed the VM attestation validator accepts the current fixture attestation and rejects missing marker, stale git SHA, host `/sys` source paths, and unsupported tuples. Zig tests, governance, release gate, contract check, and no-QEMU-leftover checks passed.
- `2026-06-12T16:26:07-04:00` — **T17 run-all VM harness delegation:** Updated `qa/vm/run_all_lab.sh` so configured non-host-safe runs delegate through `qa/vm/run_lab.sh --mode execute` and synthesize run-all stage evidence from the VM execution manifest. Host-safe mode remains unchanged. The tracked `qa/vm/lab.env` path is still a fixture, so delegated stages are truthfully labeled `vm-configured-fixture`, `release_use=false`, not real `vm-live` release proof.
- `2026-06-12T16:26:07-04:00` — **T17 false-PASS fix and verification:** Fixed the delegated run-all path so invalid VM config from the execution harness becomes top-level `REFUSE` with a nonzero exit instead of a misleading `PASS`. Verification reran syntax, fixture delegation, host-safe summary validation, tmux manual QA, malformed-env/no-config refusals, stale git SHA checks, cleanup checks, Zig tests, governance manifest, and release gate self-test.
- `2026-06-12T16:33:12-04:00` — **T17 verifier feedback resolved:** Added explicit fixture VM transcript command events and copy-out stage artifacts for delegated verifier-only, partial attach, rollback drill, and observe-partial stages. Run-all now marks delegated stages PASS only when those command events are present, treats missing delegated VM command evidence as FAIL, and clears stale files from each run-all output directory before writing new evidence.
- `2026-06-12T16:36:26-04:00` — **T17 verification complete:** Independent ultrawork verification confirmed VM-required run-all delegation after the false-PASS and stale-state fixes. The fixture path now requires explicit VM transcript command events before delegated stages are PASS, invalid VM config exits nonzero with top-level `REFUSE`, host-safe mode remains valid, stale output dirs are cleared, and fixture evidence remains non-release-eligible.
- `2026-06-12T16:44:20-04:00` — **T18 VM verifier-only evidence:** Extended the disposable VM fixture path to copy out verifier-only evidence: verifier log, parsed verifier status/reason, BPF object and metadata hashes, sched_ext state before/after, enable_seq before/after, and cgroup membership digest before/after. The verifier evidence checker now accepts `--evidence` for `zig-scheduler/verifier-only-evidence/v1` and rejects host-refusal JSON as verifier proof unless explicitly parsed as a refusal.
- `2026-06-12T16:57:08-04:00` — **T18 verification complete:** Independent ultrawork verification confirmed VM verifier-only evidence copy-out after adversarial fixes. The fixture path emits verifier log/parsed status/object hashes/state and cgroup invariants, host refusal is rejected as verifier evidence, dirty/stale/misleading/cleanup-leftover contradictions fail closed, and evidence remains fixture-only/non-release-eligible.
- `2026-06-12T17:08:59-04:00` — **T19 fixture partial attach evidence:** Added VM fixture partial-attach copy-out evidence with literal `bpftool struct_ops register` transcript proof, `ops=zigsched_minimal`, `SCX_OPS_SWITCH_PARTIAL`, rollback id/status, disabled post-state, object hash, allowlisted lab target cgroup, and `host_mutation=false`. This remains fixture evidence and `release_eligible_live_proof=false`.
- `2026-06-12T17:08:59-04:00` — **T19 verification complete:** Independent ultrawork verification confirmed positive fixture/run-all partial attach evidence, host-refusal rejection, system/root target rejection, bad ops rejection, release-proof contradiction rejection, cleanup-leftover rejection, no-host-mutation audit, governance, and diff checks.
- `2026-06-12T17:18:12-04:00` — **T20 runtime observe stream evidence:** Added observe-partial runtime sample copy-out for the disposable VM fixture path, including before/during/after sched_ext state, `ops=zigsched_minimal` during attach, workload liveness, cgroup membership digest, daemon stream fanout events, rollback/audit linkage, and summary validation. The current configured path remains `vm-configured-fixture` with `release_eligible_live_proof=false`; real `vm-live` release proof remains gated for later signed VM transport.
- `2026-06-12T17:24:34-04:00` — **T20 verifier feedback fix:** Tightened observe summary validation so fixture-shaped evidence cannot be relabeled as `vm-live` and release-eligible without explicit qemu VM-live proof fields. Direct observe generation now self-validates its summary and fails closed if environment overrides attempt an unsupported release-proof relabel.
- `2026-06-12T17:28:47-04:00` — **T20 release-proof fail-closed hardening:** After independent verifier feedback, observe summary validation now rejects every `release_eligible_live_proof=true` claim until a later signed VM-live transport proof is implemented. This prevents forged or relabeled fixture evidence from being accepted as release-eligible, even if a self-declared `vm_live_proof` object is present.
- `2026-06-12T17:41:32-04:00` — **T21 daemon rollback/stop control path:** Added typed `run_lab_vm`, `rollback_lab_run`, and `stop_lab_run` daemon handling. The daemon can create a known fixture VM lab action id, target it with rollback/stop using `target_action_id` plus rollback id, execute the fixture rollback drill, validate the audit ledger, and treat repeated rollback for the same target as `already_rolled_back`. Stale targets and stale rollback ids refuse fail-closed with `host_mutation=false`.
- `2026-06-12T18:00:42-04:00` — **T22 live lab lifecycle TUI stream:** Added VM-live lifecycle snapshot modeling and a replay fixture so the sched_ext TUI shows current stage, VM marker, runtime samples, zigsched_minimal partial attach state, rollback status, audit id, release eligibility, and incident state while preserving fail-closed/no-load wording. Added adaptive TUI row widths so VM-live lifecycle tokens remain visible at 80 and 100 columns, plus host-safe, VM-live, and CJK snapshot validation. Visual QA initially found tmux capture and 80-column clipping issues; both were fixed and reverified.
- `2026-06-12T18:15:00-04:00` — **T23 safe rollback controls in TUI:** Added interactive VM-lab arming plus test-mode stop/rollback confirmation. Rollback now refuses without known audit/rollback/target ids, dispatches typed `rollback_lab_run` with `target_action_id` only after confirmation, surfaces daemon PASS and idempotent `already_rolled_back` states, and keeps the TUI fail-closed with no shell execution. Tmux and daemon journal evidence captured the sequence `m b b b`: VM lab active, confirmation, rollback PASS, then idempotent rollback.
- `2026-06-12T18:23:19-04:00` — **T24 live behavior proof validator:** Added `qa/live_behavior_check.py` to reject attach-only scheduler proof and require a VM-live behavior bundle: VM marker, qemu VM kind, partial-switch `zigsched_minimal` metadata, allowlisted cgroup, runtime samples and daemon runtime events during attach, stable fatal/reject/fallback counters, live workload samples, rollback-restored state, audit ledger, and `host_mutation=false`. RED evidence shows the old partial attach checker accepted attach-only proof; GREEN evidence accepts a full generated VM-live bundle and rejects attach-only/counter-growth/host-mutation variants.
- `2026-06-12T18:31:01-04:00` — **T24 verifier-feedback hardening:** Independent verification found the live behavior validator still accepted malformed bundles missing before/after phases, missing dispatch/fallback/fatal counter families, malformed BPF object hashes, and missing daemon `host_mutation=false`. Added those false-PASS cases to self-test, confirmed they failed before the fix, then tightened the validator so all variants are rejected while the full VM-live behavior bundle still passes.
- `2026-06-12T18:38:02-04:00` — **T25 release gate live-proof integration:** Release self-test now covers VM-live behavior proof acceptance and surrogate attach-only rejection. The release gate now writes SKIP without approval when VM-live behavior proof is missing, and only creates a controlled-lab candidate approval when `qa/live_behavior_check.py` accepts the configured behavior bundle. Governance docs now state that host-safe/surrogate CI evidence cannot be relabeled as live proof.
- `2026-06-12T18:52:30-04:00` — **T26 VM-live stress/runtime linkage:** Added stress/chaos validation that requires VM-live stress summaries to link to runtime samples, latency/fairness series, rollback PASS, and stable fallback/reject counters. The VM lab run-all order now observes runtime samples before stress and passes the observe/DSQ artifacts into the stress stage; host-safe runs remain surrogate-only and still avoid host scheduler mutation.
- `2026-06-12T19:05:20-04:00` — **T27 package lifecycle daemon safety:** Extended the staged package lifecycle drill so install/upgrade/uninstall now checks the daemon binary, daemon systemd unit, default config, mutation gating, evidence archive preservation, and confirms no daemon or mutation service is enabled. RED evidence showed the previous lifecycle accepted a daemon unit with `WantedBy=multi-user.target`; GREEN evidence rejects that variant and passes the staged install/upgrade/uninstall drill.
- `2026-06-12T19:15:00-04:00` — **T28 incident/fallback drill surfaced in TUI:** Added a controlled incident drill path that simulates verifier rejection, scheduler process exit, lost runtime stream, rollback completion, and fallback completion through the typed daemon/TUI action surface. The daemon now emits an `INCIDENT` event for `incident_drill`, preserves rollback/audit evidence from the VM-only/controlled fixture drill, and the TUI keeps `INCIDENT rollback/fallback drill` visible even after the later PASS completion event. The drill does not fire SysRq on the host and records `host_mutation=false` throughout.
- `2026-06-12T19:18:00-04:00` — **T29 non-VM operation gate ADR:** Added ADR 0003 as a proposed/not implemented design gate. Non-VM scheduler mutation remains refused until a future explicit approval supplies kernel tuple evidence, verifier logs, live behavior proof, rollback and incident drills, audit ledger validation, security signoff, package safety proof, and updated governance/release approval.
- `2026-06-12T19:24:00-04:00` — **T30 TUI-driven workflow docs:** Refreshed README, VM lab runbook, fallback runbook, verifier/incident runbook, packaging notes, and security threat notes with the TUI-to-daemon workflow, key map, exact manual QA commands, evidence paths, and continued host-safety boundaries.
- `2026-06-12T19:36:00-04:00` — **T31 blocked on real VM-live artifact:** Added `qa/tui_live_lab_e2e.sh`, a fail-closed TUI-driven e2e harness that refuses completion without a `qa/live_behavior_check.py`-accepted VM-live bundle and only prints `LAB RUN COMPLETE rolled_back=true live_behavior=PASS` when rollback is observed and the bundle validates. Independent verification confirmed this environment has no VM image/kernel config and no existing VM-live summary; T31 remains incomplete rather than relabeling host-safe or fixture evidence.

## 2026-06-13T00:23:52Z — Local microVM live sched_ext lab setup
- Installed/verified local QEMU/KVM prerequisite and added `qa/vm/run_microvm_live_lab.sh` for disposable microVM lab runs.
- Updated `zigsched_minimal` BPF object to modern libbpf map/struct_ops declarations and verified real guest-side `bpftool struct_ops register` succeeds for `zigsched_minimal`.
- Generated a VM-live behavior bundle under `evidence/lab/run-all/microvm-live-*` and validated it with `qa/live_behavior_check.py`.
- Fed the VM-live bundle through `qa/tui_live_lab_e2e.sh`; result: `LAB RUN COMPLETE rolled_back=true live_behavior=PASS`.
- Scope remains lab-only: no arbitrary-host attach path or production deployment claim was added.
- `2026-06-14T02:18:46-04:00` — **T17 live microVM daemon lifecycle translation:** Wired the daemon `run_lab_microvm_live` path into the trusted microVM runner, added early queued/active stream events before the runner subprocess completes, translated accepted VM-live bundles into UI lifecycle events, and rejected stale/malformed live bundles with `unsafe_to_assume`. In this host environment the manual daemon run created a fresh run directory and ended with explicit `REFUSE` reason `qemu_not_found`, with every event reporting `host_mutation=false`.
- `2026-06-14T02:26:39-04:00` — **T17 verifier feedback resolved:** Pre-validates all live microVM summary stage statuses before emitting any PASS lifecycle events, adds a regression test proving malformed stage status leaves the stream untouched, and updates daemon-output parsing so terminal microVM runner `REFUSE`/`SKIP` reasons are visible instead of ambiguous host-safe refusal. Fresh verification passes Zig tests, unsafe CLI matrix, no-host mutation audit, PTY smoke, fmt, and diff checks.
- `2026-06-17T11:39:30-04:00` — **T18 rollback/stop/failure matrix:** Hardened the TUI and daemon QA matrix for rollback confirmation, stop confirmation, duplicate action IDs, stale rollback targets, stale rollback IDs, incident rollback/fallback drills, and microVM runner refusal. The PTY smoke now drives `m b b`, `m s s`, stale/duplicate daemon actions, and verifies safe refusal or rollback-safe completion without host mutation.
- `2026-06-17T11:49:04-04:00` — **T18 verifier feedback resolved:** Independent verification found rollback/stop confirmation only applied in test mode. The operator TUI now requires the same two-key rollback and stop confirmation in normal interactive mode, adds regression coverage for non-test interactive confirmation, and refreshes T18 PTY, daemon rollback/idempotence, unsafe/no-host, governance, fmt, and diff evidence.
- `2026-06-17T11:55:05-04:00` — **T19 live VM TUI entrypoint and help clarity:** Added the first-class `zig build tui-live-vm` step, wired it to the VM-lab TUI with a safe daemon state directory and daemon binary, and added `zig build tui-live-vm -- --help` usage that lists prerequisites without launching QEMU. Root help now explains that `zig build run` remains fail-closed, points operators to `zig build tui-live-vm`, and states that unsafe host mutation is refused.
- `2026-06-17T12:00:14-04:00` — **T19 verifier feedback resolved:** Tightened the T19 help contract so `zig build --help`, root CLI help, and `zig build tui-live-vm -- --help` all name the first-class live VM TUI entrypoint while preserving the fail-closed/VM-gated boundary. The TUI help now prints the installed-binary command shape and prerequisites only; help mode exits before any QEMU, daemon, or TUI process launch.
- `2026-06-17T12:20:52-04:00` — **T20 TUI-launched live microVM e2e harness:** Replaced the old pre-existing-bundle completion path with strict `qa/tui_live_lab_e2e.sh --mode launch-live-vm`, which refuses supplied `--live-bundle`/`ZIG_SCHEDULER_LIVE_BEHAVIOR_BUNDLE`, drives the TUI through a PTY so key `m` dispatches `run_lab_microvm_live`, extracts only daemon-emitted generated bundle paths, validates live behavior plus freshness before printing `LAB RUN COMPLETE`, and otherwise reports explicit SKIP/REFUSE with `host_mutation=false`. In this host environment the strict run refuses safely at `qemu_not_found` and does not print PASS.
- `2026-06-17T12:53:26-04:00` — **T20 verifier blocker fix for live QEMU visibility:** Threaded the daemon parent environment into the live microVM env builder so the child runner can recover an explicit `ZIG_SCHEDULER_QEMU_BIN` from the daemon’s own PATH/HOME before it sanitizes the child environment. The strict T20 proof now advances past `qemu_not_found` and fails later with a truthful host-prep refusal (`nix_busybox_unavailable`) instead of hiding visible QEMU behind the clean env.
