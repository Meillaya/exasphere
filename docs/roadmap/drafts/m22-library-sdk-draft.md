# M22 library / SDK draft inventory

This draft was the planning-stage precursor to the committed stable inventory in:
- `docs/m22-library-sdk.md`

Keep it only as historical planning context. The current source of truth for the
stable embedder subset is `docs/m22-library-sdk.md`.
